# Core scientific stack — used by both notebooks
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
torch>=2.0

# TabPFN v2 — used only by feature_head_scoring_multi_dataset.ipynb
# (the nanoTabPFN notebook needs no extra package; its model.py and weights
#  are bundled under src/ and checkpoints/).
tabpfn>=2.0
python-dotenv>=1.0

# Notebook runtime
jupyter
ipykernel
